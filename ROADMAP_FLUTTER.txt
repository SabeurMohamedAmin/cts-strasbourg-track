================================================================
 CTS STRASBOURG TRACK - ROADMAP: WEB (NUXT 4) + MOBILE (FLUTTER)
 Note: Flutter targets ANDROID / IOS ONLY. Web = Nuxt/Vuetify.
 See ROADMAP_NITRO_API.txt for exposing the Nitro API to mobile.
================================================================
Legend:  [x] done   [~] in progress   [ ] todo

----------------------------------------------------------------
PHASE 0 - CURRENT STATE
----------------------------------------------------------------
[x] Nuxt 4 + Vuetify web app running
[x] GTFS theoretical data available (Donnees theoriques)
[x] Server layer exists (server/)
[x] Quality gates run locally - no pipelines rule (see issue #1)
[x] Mirrored on GitHub (SabeurMohamedAmin/cts-strasbourg-track)

----------------------------------------------------------------
PHASE 1 - FOUNDATION & SHARED API LAYER
----------------------------------------------------------------
[ ] 1.1  List every place the app calls CTS / GTFS data
[ ] 1.2  Move ALL transit logic into Nitro server routes (server/api/)
[ ] 1.3  Keep CTS API keys server-side only (.env, never in client)
[ ] 1.4  Add caching on real-time endpoints (respect CTS rate limits)
[ ] 1.5  Write an OpenAPI spec describing every endpoint
[ ] 1.6  Version the API (/api/v1/...)
[ ] 1.7  Add basic API tests in tests/
[~] 1.8  WEB ALIGNMENT GATE: web app consumes ONLY /api/v1
         (moved from Phase 2.1 - must happen BEFORE the contract
         freeze, so both clients share one surface)
         2026-08-19 (dev-mobile): app/utils/api.ts now holds the only
         copy of the prefix. apiV1() is used by useStopArrivals,
         useNearestStops, useStationSchedule, useAddressSearch,
         useFavoriteArrivals, useVehicleStream (SSE + REST fallback),
         the stops and lines stores, both blog pages and pages/index.vue.
         Admin stays unversioned on purpose: /api/v1/admin/** is 404.
         No direct fetch found in useStopSearch, useNextDepartures,
         useStationLines, stores/vehicles, useMapFraming, useNetworkLayers,
         useFocusStationsArea, useLandmarkLayer, StopSheet, MapView,
         prefetch-station.client (reuses stationScheduleUrl), pages/live,
         station/[slug], trajet, favoris/index and favoris/gerer: they all
         read through the migrated stores and composables.
         Remaining before ticking [x]:
           grep -rn "'/api/" app/ --include=*.ts --include=*.vue
         (large SFCs were only partly audited by hand), then deprecate
         unversioned /api/* for browsers as NITRO 2.4 describes.
         FOUND: /api/eurometropole/bounds has NO web caller. The map frame
         is the static bbox in app/utils/map-constants.ts, so the endpoint
         is only useful to Flutter, if at all.
         NOTE: Phase 1.1-1.7 above are still [ ] here but
         ROADMAP_NITRO_API records them as done. This file exists in BOTH
         repos and the two copies have drifted.

----------------------------------------------------------------
PHASE 2 - WEB APP POLISH (PWA)
----------------------------------------------------------------
[ ] 2.1  MOVED to Phase 1.8 (web alignment gate) - web must be
         on /api/v1 before the contract freeze
[ ] 2.2  Add @vite-pwa/nuxt (installable app, service worker)
[ ] 2.3  Cache stops & lines offline
[ ] 2.4  Mobile-first responsive audit (Vuetify breakpoints)
[ ] 2.5  Web push notifications for line disruptions
[ ] 2.6  Lighthouse pass: performance + accessibility > 90

----------------------------------------------------------------
PHASE 3 - FLUTTER PROJECT SETUP (ANDROID / IOS ONLY)
----------------------------------------------------------------
[ ] 3.0  READINESS GATE (moved from ROADMAP_FIXES #11, 2026-08-18).
         Re-verify ALL sub-items before flutter create:
         [ ] CTS SIRI token in hand + TOS permits app use
             (token received May 2026 - TOS review still pending)
         [ ] PWA deployed on Northflank + 50+ real users observed
         [x] docs/API.md written + frozen (regenerate from OpenAPI
             once NITRO Step 2 lands - keep one contract source)
         [x] Non-infringing name chosen + updated in all files
             (Strasbourg Bus-Trams Live)
         [x] Data attribution present in UI
         [ ] /api/stops, /api/vehicles, /api/stream/vehicles tested
             from a mobile browser
         [ ] 14-day production stability confirmed (FIXES #9)
         [ ] GTFS fallback tested and working (FIXES #10)
         [x] All FIXES P0 items completed (layout deduped, interval
             fixed, lang set)
[ ] 3.1  Install Flutter SDK + Android Studio + Xcode
[ ] 3.2  Create flutter app (mobile/ folder or separate repo)
[ ] 3.3  Choose state management (Riverpod recommended)
[ ] 3.4  Feature-first folder structure (features/stops, features/lines...)
[ ] 3.5  Generate Dart API client from the OpenAPI spec
[ ] 3.6  Set up flavors: dev / staging / prod
[ ] 3.7  Material 3 theme matching the web app colors
[ ] 3.8  Run flutter analyze + flutter test locally before each MR
         (no pipeline - see issue #1)
[ ] 3.9  Build debug APK locally (flutter build apk --debug)
         before each MR
[ ] 3.10 Dart client sends X-App-Token header
         (see ROADMAP_NITRO_API Step 3.3)

----------------------------------------------------------------
PHASE 4 - CORE MOBILE FEATURES (MVP)
----------------------------------------------------------------
[ ] 4.1  Stop search (name / line)
[ ] 4.2  Nearby stops via geolocation permission
[ ] 4.3  Real-time arrivals screen for a stop
[ ] 4.4  Line detail screen (route, direction, schedule)
[ ] 4.5  Map screen (flutter_map + OpenStreetMap)
[ ] 4.6  Show live vehicle positions on the map
[ ] 4.7  Favorites (local storage: Hive or Drift)
[ ] 4.8  Recent stops history
[ ] 4.9  Error / offline states for every screen
[ ] 4.10 MVP internal release (APK + TestFlight)
[ ] 4.11 Blog screen (list + article) via /api/v1/blog
         - feature parity with web /blog

----------------------------------------------------------------
PHASE 5 - MOBILE-NATIVE VALUE
----------------------------------------------------------------
[ ] 5.1  Firebase Cloud Messaging setup (Android + iOS)
[ ] 5.2  Push notifications for disruptions on favorite lines
[ ] 5.3  Home screen widget: next departures for favorite stop
[ ] 5.4  Offline mode: cached GTFS schedules fallback
[ ] 5.5  Deep links (share a stop/line, open from web URL)
[ ] 5.6  Dark mode + localization (v1 = fr only on BOTH clients;
         en added to both simultaneously later)
[ ] 5.7  Disruptions banner via /api/v1/disruptions
         (same source as web banner + push)

----------------------------------------------------------------
PHASE 6 - RELEASE & ITERATE
----------------------------------------------------------------
[ ] 6.1  App signing: Android keystore + iOS certificates
[ ] 6.2  Fastlane setup for both stores
[ ] 6.3  Release builds produced locally via Fastlane on tags
         (no pipeline - see issue #1, ties into 6.2)
[ ] 6.4  Play Store internal track beta
[ ] 6.5  TestFlight beta
[ ] 6.6  Crash reporting (Sentry or Crashlytics)
[ ] 6.7  Analytics on key screens
[ ] 6.8  Accessibility pass (TalkBack / VoiceOver)
[ ] 6.9  Store listings (screenshots, descriptions FR/EN)
[ ] 6.10 Public release v1.0

----------------------------------------------------------------
RULES
----------------------------------------------------------------
- Single source of truth = the /api/v1 backend. No transit logic in clients.
- i18n: v1 ships fr only on BOTH clients; en is added to both
  simultaneously later (aligns 5.6 with the web's fr-only rule).
- Feature parity is tracked in ROADMAP_PARITY.md.
- Phase 4 alone is a shippable MVP. Ship early, iterate.
- Update this file as you go: [ ] -> [~] -> [x]
